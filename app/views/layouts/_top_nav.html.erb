<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Alpha Blog</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to 'Bloggers', users_path, class: 'nav-link' %>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarArticlesMenu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Articles
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarArticlesMenu">
          <%= link_to 'List articles', articles_path, class: "dropdown-item" %>
          <% if logged_in? %>
            <%= link_to 'Create new article', new_article_path, class: "dropdown-item" %>
          <% end %>
        </div>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarArticlesMenu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Categories
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarArticlesMenu">
          <%= link_to 'View categories', categories_path, class: "dropdown-item" %>
          <% if logged_in? && current_user.admin? %>
            <%= link_to 'Create new category', new_category_path, class: "dropdown-item" %>
          <% end %>
        </div>
      </li>

      <li class="nav-item dropdown">
        <% if logged_in? %>
          <a class="nav-link dropdown-toggle" href="#" id="navbarUserMenu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= gravatar_for(current_user, size: 25, display: 'inline') %>
            <%= current_user.username %><%= ' (Admin)' if current_user.admin? %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarUserMenu">
            <%= link_to 'View Profile', user_path(current_user), class: "dropdown-item" %>
            <%= link_to 'Edit Profile', edit_user_path(current_user), class: "dropdown-item" %>
            <div class="dropdown-divider"></div>
            <%= link_to 'Log Out', logout_path, class: "dropdown-item", method: :delete %>
            <div class="dropdown-divider"></div>
            <%= link_to 'Delete User', user_path(current_user), class: "dropdown-item text-danger", method: :delete, data: { confirm: 'Are you sure?' } %>
          </div>
          <li class="nav-item">
            <%= link_to 'Log Out', logout_path, class: 'nav-link', method: :delete %>
          </li>
        <% else %>
          <a class="nav-link dropdown-toggle" href="#" id="navbarUserMenu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            User
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarUserMenu">
            <%= link_to 'Log In', login_path, class: "dropdown-item" %>
            <%= link_to 'Sign Up', signup_path, class: "dropdown-item" %>
          </div>
        <% end %>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>
  </div>
</nav>
